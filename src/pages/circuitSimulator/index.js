import React, { Component } from 'react';
import CircuitSimulator from '../../lib/circuit.im'

class CircuitSimulatorPage extends Component {
    render() {
        return (
            <div>
                <CircuitSimulator/>
      </div>
        );
    }
}

export default CircuitSimulatorPage;
